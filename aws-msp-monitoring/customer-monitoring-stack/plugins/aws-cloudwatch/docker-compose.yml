networks:
  default:
    external: false
services:
  aws_cloudwatch:
    container_name: aws_cloudwatch
    environment:
    - SERVICE_NAME=${SERVICE_NAME}
    - SERVICE_ENABLED=${SERVICE_ENABLED}
    healthcheck:
      interval: 60s
      retries: 3
      start_period: 60s
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:9214/health
      timeout: 10s
    image: alpine:latest
    networks:
    - default
    ports:
    - 9214:9214
    restart: unless-stopped
    volumes:
    - ./aws-cloudwatch-config.yml:/config/config.yml:ro
    - ./logs:/var/log:rw
