networks:
  default:
    external: false
services:
  alertlogic:
    container_name: alertlogic
    environment:
    - SERVICE_NAME=${SERVICE_NAME}
    - SERVICE_ENABLED=${SERVICE_ENABLED}
    healthcheck:
      interval: 60s
      retries: 3
      start_period: 60s
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:9210/health
      timeout: 10s
    image: alpine:latest
    networks:
    - default
    ports:
    - 9210:9210
    restart: unless-stopped
    volumes:
    - ./alertlogic-config.yml:/config/config.yml:ro
    - ./logs:/var/log:rw
