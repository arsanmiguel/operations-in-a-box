description: Comprehensive configuration for aws-cloudtrail
enabled: true
integration:
  authentication:
    password: ${SERVICE_PASSWORD}
    type: basic
    username: ${SERVICE_USERNAME}
  endpoints:
  - http://localhost:8080
monitoring:
  health_checks: true
  log_level: INFO
  metrics_enabled: true
name: Aws Cloudtrail
service:
  host: 0.0.0.0
  max_connections: 100
  port: 9214
  timeout: 30
version: 1.0.0
