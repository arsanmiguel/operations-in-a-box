networks:
  default:
    external: false
services:
  cicd_monitoring:
    container_name: cicd_monitoring
    environment:
    - SERVICE_NAME=${SERVICE_NAME}
    - SERVICE_ENABLED=${SERVICE_ENABLED}
    healthcheck:
      interval: 60s
      retries: 3
      start_period: 60s
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:9215/health
      timeout: 10s
    image: alpine:latest
    networks:
    - default
    ports:
    - 9215:9215
    restart: unless-stopped
    volumes:
    - ./cicd-monitoring-config.yml:/config/config.yml:ro
    - ./logs:/var/log:rw
