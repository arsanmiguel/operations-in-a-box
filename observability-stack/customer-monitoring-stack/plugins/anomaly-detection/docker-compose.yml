networks:
  default:
    external: false
services:
  anomaly_detection:
    container_name: anomaly_detection
    environment:
    - SERVICE_NAME=${SERVICE_NAME}
    - SERVICE_ENABLED=${SERVICE_ENABLED}
    healthcheck:
      interval: 60s
      retries: 3
      start_period: 60s
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:9217/health
      timeout: 10s
    image: alpine:latest
    networks:
    - default
    ports:
    - 9217:9217
    restart: unless-stopped
    volumes:
    - ./anomaly-detection-config.yml:/config/config.yml:ro
    - ./logs:/var/log:rw
