networks:
  default:
    external: false
services:
  zendesk:
    container_name: zendesk
    environment:
    - SERVICE_NAME=${SERVICE_NAME}
    - SERVICE_ENABLED=${SERVICE_ENABLED}
    healthcheck:
      interval: 60s
      retries: 3
      start_period: 60s
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:9207/health
      timeout: 10s
    image: alpine:latest
    networks:
    - default
    ports:
    - 9207:9207
    restart: unless-stopped
    volumes:
    - ./zendesk-config.yml:/config/config.yml:ro
    - ./logs:/var/log:rw
