networks:
  default:
    external: false
services:
  aws_support:
    container_name: aws_support
    environment:
    - SERVICE_NAME=${SERVICE_NAME}
    - SERVICE_ENABLED=${SERVICE_ENABLED}
    healthcheck:
      interval: 60s
      retries: 3
      start_period: 60s
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:9211/health
      timeout: 10s
    image: alpine:latest
    networks:
    - default
    ports:
    - 9211:9211
    restart: unless-stopped
    volumes:
    - ./aws-support-config.yml:/config/config.yml:ro
    - ./logs:/var/log:rw
