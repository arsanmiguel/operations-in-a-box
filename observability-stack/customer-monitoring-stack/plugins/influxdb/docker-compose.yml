networks:
  default:
    external: false
services:
  influxdb:
    container_name: influxdb
    environment:
    - SERVICE_NAME=${SERVICE_NAME}
    - SERVICE_ENABLED=${SERVICE_ENABLED}
    healthcheck:
      interval: 60s
      retries: 3
      start_period: 60s
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:9208/health
      timeout: 10s
    image: alpine:latest
    networks:
    - default
    ports:
    - 9208:9208
    restart: unless-stopped
    volumes:
    - ./influxdb-config.yml:/config/config.yml:ro
    - ./logs:/var/log:rw
