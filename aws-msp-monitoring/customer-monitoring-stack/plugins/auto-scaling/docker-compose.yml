networks:
  default:
    external: false
services:
  auto_scaling:
    container_name: auto_scaling
    environment:
    - SERVICE_NAME=${SERVICE_NAME}
    - SERVICE_ENABLED=${SERVICE_ENABLED}
    healthcheck:
      interval: 60s
      retries: 3
      start_period: 60s
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:9212/health
      timeout: 10s
    image: alpine:latest
    networks:
    - default
    ports:
    - 9212:9212
    restart: unless-stopped
    volumes:
    - ./auto-scaling-config.yml:/config/config.yml:ro
    - ./logs:/var/log:rw
